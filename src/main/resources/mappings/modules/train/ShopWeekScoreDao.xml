<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.training.modules.train.dao.ShopWeekScoreDao">
	
	<insert id="insertOfficeWeekScore" parameterType="map">
		INSERT INTO train_office_week_score(week_avg_score,area_rank,week,year,office_id,area_id)
		VALUES(#{week_avg_score},#{area_rank},#{week},#{year},#{office_id},#{area_id})
	</insert>
</mapper>