<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.training.modules.train.dao.TrainsVersionDao">

	<!-- 查询所有数据 -->
	<select id="findList" resultType="Version">
		SELECT 
		  id,
		  versionCode,
		  del_flag AS Flag,
		  add_time AS addTime,
		  remark,
		  type,
		  client
		FROM
		  train_version
		ORDER BY add_time DESC
	</select>
	
	<!-- 根据id查看 -->
	<select id="get" resultType="Version">
		SELECT 
		  id,
		  versionCode,
		  del_flag AS Flag,
		  add_time AS addTime,
		  remark,
		  type,
		  client
		FROM
		  train_version
		WHERE id = #{id}
	</select>
	
	<!-- 保存 -->
	<insert id="insert">
		INSERT INTO train_version(
            versionCode,
			del_flag,
			add_time,
			remark,
			type,
			client
		)VALUES(
		    #{versionCode},
		    #{Flag},
		    SYSDATE(),
	        #{remark},
	        #{type},
	        #{client}
	    )
	</insert>
	
	<!-- 修改 -->
	<update id="update">
		UPDATE train_version
			SET 
			  versionCode = #{versionCode},
			  remark = #{remark},
			  type   = #{type},
			  del_flag = #{Flag},
			  client = #{client}
			WHERE id = #{id}
	</update>
	
</mapper>