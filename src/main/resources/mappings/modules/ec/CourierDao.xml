<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.training.modules.ec.dao.CourierDao">

	<sql id="actionColunm">
		a.courierId,
		a.starting_point,
		a.courier_name,
		a.type,
		a.default_weight,
		a.default_price,
		a.default_add,
		a.default_addPrice,
		a.create_date,
		a.del_flag
	</sql>
	<!-- 根据id获取数据 -->
	<select id="get" resultType="Courier">
		SELECT
		 <include refid="actionColunm"></include>
		FROM mtmydb.mtmy_courier a
		WHERE a.courierId=#{courierId}
	</select>
	<!-- 分页查询 -->
	<select id="findList" resultType="Courier">
		SELECT
		 <include refid="actionColunm"></include>
		FROM mtmydb.mtmy_courier a
		WHERE 1=1 and a.del_flag=0
		ORDER BY a.create_date DESC
	</select>
	<select id="findAllList" resultType="Courier">
		SELECT
		 <include refid="actionColunm"></include>
		FROM mtmydb.mtmy_courier a
	</select>
	
	<!-- 更新状态 -->
	<update id="updateStatus">
		UPDATE mtmydb.mtmy_courier SET
			del_flag=1
		where courierId=#{courierId}
	</update>

	<!-- 插入数据 -->
	<insert id="insertCourier">
		INSERT INTO mtmydb.mtmy_courier
		(
		starting_point,
		courier_name,
		type,
		create_date
		)VALUES
		(
		#{startingPoint},
		#{courierName},
		#{type},
		SYSDATE()
		)

	</insert>
	
</mapper>